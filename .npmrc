; react-helmet-async@2 declares a conservative peer range (React 16–18) but
; works correctly with React 19 at runtime. legacy-peer-deps lets npm (locally
; and on Vercel) install it against React 19 without an ERESOLVE failure.
legacy-peer-deps=true
